version: '3'
services:
  prometheus:
    image: quay.io/prometheus/prometheus:v2.33.1
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - /etc/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml  # Corrected volume mapping
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
    networks:
      - monitoring_net  # Add this line


  node-exporter:
    image: quay.io/prometheus/node-exporter:v1.3.1  
    container_name: node-exporter
    volumes:
      - /etc/prometheus/node_exporter.yml:/etc/prometheus/node_exporter.yml  # Mount the node_exporter configuration file
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - "--collector.textfile.directory=/etc/prometheus"  
    ports:
      - "9100:9100"
    networks:
      - monitoring_net  # Add this line

  grafana:
    image: grafana/grafana
    container_name: grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
    
    volumes:
      - grafana-data:/var/lib/grafana

    depends_on:
      - prometheus
   
    networks:
      - monitoring_net  
  
 


  timescaledb:
    image: timescale/timescaledb-ha:pg14-latest
    container_name: timescaledb
    environment:
      - POSTGRES_PASSWORD=mypassword  
      - POSTGRES_DB=mydatabase  
      - POSTGRES_USER=myuser  
      
    ports:
      - "5432:5432"
    volumes:
      - timescaledb_data:/home/postgres/pgdata/data/
    networks:
      - monitoring_net

networks:
  monitoring_net:
    driver : bridge  


volumes:
  grafana-data:
  timescaledb_data:
